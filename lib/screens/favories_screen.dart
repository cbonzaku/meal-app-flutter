import 'package:flutter/material.dart';

class FavoriScreen extends StatelessWidget {
  const FavoriScreen({ Key? key }) : super(key: key);
  static const title="favoris";

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}